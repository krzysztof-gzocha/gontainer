meta:
  pkg: container
  container_type: MyFooContainer
  imports:
    "example": "mymodule"

parameters:
  name: Jane\/\t
  first_name: "%name%"
  last_name: "Doe"
  age: 30
  salary: '%envInt("PERSON_SALARY")%'
  position: '%env("PERSON_POSITION")%'
  multiline: |
    multi
    line

services:
  anotherTest:
    constructor: "example/pkg.NewEmplWithErr"
    with_error: true
    getter: GetMyErr
    type: "*example/pkg.Employee"

  testPerson:
    type: "example/pkg.Employee"
    getter: GetFooPerson

  person:
    constructor: "example/pkg.NewPerson"
    args: ["%first_name% %last_name% (%age%)", "%age%"]
    getter: "GetPerson"
    type: "*example/pkg.Person"
    disposable: true

  employee:
    getter: "GetEmployee"
    type: "*example/pkg.Employee"
    # todo factory e.g. "@myFactoryService.NewEmployee"
    # todo rename constructor to factory
    # todo calls https://symfony.com/doc/current/service_container/calls.html
    # todo add syntax "import.Employee{}" and "&import.Employee{}" or use type from line 28?
    constructor: "example/pkg.NewEmployee"
    args:
      - "@person.(*example/pkg.Person)"
      - "%salary%"
      - "%position%"
      - "@testPerson"
      - "@testPerson"
      - "@testPerson"
